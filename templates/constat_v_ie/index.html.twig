<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulaire de constat de décès</title>
    
    <style>
       
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
            position: relative;
            min-height: 100vh; /* Ensure the body covers the entire viewport height */
        }
        .container {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h2 {
            font-size: 24px;
            color: #333;
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            color: #666;
        }
        input[type="text"],
        input[type="date"] {
            width: calc(100% - 12px);
            padding: 8px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 3px;
            font-size: 16px;
            box-sizing: border-box;
        }
        input[type="text"]:required,
        input[type="date"]:required {
            border-color: blue;
        }
        input[type="submit"] {
            width: 100%;
            padding: 10px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 3px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s;
        }
        input[type="submit"]:hover {
            background-color: blue;
        }
        .heart-message {
            background-color: #ff4d4d; 
            padding: 15px;
            border-radius: 10px;
            text-align: center;
            margin-top: 20px;
        }
        .heart {
            color: #fff; 
            font-size: 24px;
            margin-right: 5px;
        }
        .logo-container {
            position: absolute;
            bottom: 20px;
            left: 20px;
        }
        .logo {
            max-width: 150px;
            height: auto;
            border: 2px solid blue; /* Example border */
            border-radius: 5px; /* Example border radius */
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.2); /* Example box shadow */
        }





        .navbar {
            background-color: #666;
            overflow: hidden;
        }

        .navbar a {
            float: left;
            display: block;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 20px;
            text-decoration: none;
        }

        .navbar a:hover {
            background-color: #ddd;
            color: black;
        }

        .navbar a.active {
            background-color: blue;
            color: white;
        }

        /* Search bar styles */
        .search-container {
            float: right;
            margin-right: 20px;
        }

        .search-container input[type=text] {
            padding: 6px;
            margin-top: 8px;
            font-size: 17px;
            border: none;
        }

        .search-container button {
            float: right;
            padding: 6px 10px;
            margin-top: 8px;
            margin-right: 16px;
            background: #ddd;
            font-size: 17px;
            border: none;
            cursor: pointer;
        }

        
    </style>
</head>
<body>

<div class="navbar">
  <a class="active" href="landingpage">Home</a>
  <a href="#">About</a>
  <a href="#">Services</a>
  <a href="#">Contact</a>
  <div class="search-container">
    <form action="/action_page.php">
        <input type="text" placeholder="Search..." name="search">
       
    </form>
</div>
</div>

<div class="logo-container">
    <img src="../assets/img/about/1.png" alt="logo" class="logo">
</div>

<div class="container">
    <h2>Formulaire de constat de décès</h2>

    {{ form_start(form, {'action': path('app_constat_vie')}) }}

        {{ form_row(form.nom) }}
        {{ form_row(form.prenom) }}
        {{ form_row(form.CIN) }}
        {{ form_row(form.DateDeDeces) }}
        {{ form_row(form.CauseDeDeces) }}
        {{ form_row(form.IdentifiantDeLinformant) }}

        <input type="submit" value="Envoyer" style="background-color:blue" formaction="{{ path('app_constatvie_add') }}">
    {{ form_end(form) }}
    <div class="heart-message">
        <span class="heart">&hearts;</span> Lbraka fikom 
    </div>
</div>

</body>
</html>
